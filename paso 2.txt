/* Estilos para los XV Años de Melanie - Temática La Princesa y el Sapo */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Georgia', serif;
}

body {
    background-color: #f2f7f4; /* Fondo verde sumamente bajito, muy fresco */
    color: #1a3a2a; /* Texto verde oscuro esmeralda */
}

/* Encabezado Mágico (Fondo degradado Bosque Encantado/Lila) */
.hero {
    background: linear-gradient(135deg, #0e301d 0%, #311847 100%); /* Verde esmeralda profundo a lila oscuro */
    color: #f7ebff;
    text-align: center;
    padding: 110px 20px;
}

.hero-content h1 {
    font-size: 3.8rem;
    font-weight: normal;
    color: #ffffff;
    text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
    margin: 15px 0;
}

.hero-content .subtitle {
    font-size: 1.3rem;
    text-transform: uppercase;
    letter-spacing: 5px;
    color: #ffdceb; /* Toque muy sutil de rosa pastel */
}

.hero-content .phrase {
    font-style: italic;
    font-size: 1.15rem;
    color: #dfc5fe; /* Lavanda claro */
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.6;
}

.divider {
    font-size: 1.5rem;
    margin-top: 25px;
}

/* Estilos de las Secciones */
.section {
    padding: 60px 20px;
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
}

h2 {
    font-size: 2.1rem;
    color: #124027; /* Verde esmeralda */
    margin-bottom: 25px;
}

h2::after {
    content: "🌸"; /* Flor de loto/lirio simulada */
    display: block;
    font-size: 1.1rem;
    color: #b388eb; /* Detalle lavanda */
    margin-top: 8px;
}

/* Contenedor Cuenta Regresiva */
.countdown-container {
    background-color: #ffffff;
    border-radius: 20px;
    box-shadow: 0 8px 24px rgba(18, 64, 39, 0.06);
    margin-top: -40px;
    position: relative;
    z-index: 10;
    width: 85%;
}

.countdown {
    display: flex;
    justify-content: center;
    gap: 15px;
}

.time-box {
    background-color: #e6f2eb; /* Verde muy bajito */
    border: 1px solid #d0e6da;
    padding: 15px 20px;
    border-radius: 12px;
    min-width: 95px;
}

.time-box span {
    font-size: 2.3rem;
    font-weight: bold;
    color: #5c357a; /* Números en lila/lavanda oscuro */
    display: block;
}

.time-box p {
    font-size: 0.85rem;
    color: #124027;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* Tarjetas Informativas */
.cards-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 30px;
    margin-top: 15px;
}

.card {
    background: #ffffff;
    border-top: 5px solid #124027; /* Línea verde esmeralda arriba */
    border-radius: 12px;
    padding: 30px;
    width: 340px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.04);
}

.card-icon {
    font-size: 2.2rem;
    margin-bottom: 12px;
}

.card h3 {
    font-size: 1.4rem;
    color: #5c357a; /* Título lavanda oscuro */
    margin-bottom: 15px;
}

.card p {
    margin-bottom: 8px;
    font-size: 1rem;
    color: #333;
}

.card .time {
    font-weight: bold;
    color: #124027;
}

/* Botones */
.btn {
    display: inline-block;
    background-color: #124027;
    color: white;
    text-decoration: none;
    padding: 10px 22px;
    border-radius: 20px;
    margin-top: 15px;
    font-size: 0.9rem;
    transition: background 0.3s;
}

.btn:hover {
    background-color: #1f633f;
}

.btn-whatsapp {
    background-color: #5c357a; /* Lavanda oscuro para destacar */
    padding: 14px 35px;
    font-size: 1.1rem;
    border-radius: 30px;
    box-shadow: 0 4px 12px rgba(92, 53, 122, 0.2);
}

.btn-whatsapp:hover {
    background-color: #744399;
}

/* Bloques inferiores de detalles */
.details-section {
    background-color: #f4eoff; /* Bloque de fondo lila suave */
    max-width: 100%;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 40px;
    border-radius: 0;
}

.detail-box {
    max-width: 380px;
    padding: 10px;
}

.detail-box h3 {
    color: #124027;
    margin-bottom: 12px;
    font-size: 1.3rem;
}

.detail-box .note {
    color: #555;
    font-size: 0.9rem;
    margin-top: 5px;
    font-style: italic;
}

/* Pie de página */
footer {
    background-color: #0b2417; /* Verde casi negro */
    color: #dfc5fe;
    text-align: center;
    padding: 25px;
    font-size: 0.85rem;
}